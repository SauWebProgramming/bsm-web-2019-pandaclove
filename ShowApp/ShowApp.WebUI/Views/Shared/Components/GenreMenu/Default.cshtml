@model IEnumerable<Genre>

<h4 class="my-4">Genres</h4>
<div class="list-group">
    @foreach (var item in Model)
    {
        <a asp-action="Index" 
           asp-route-orderBy="Genre" 
           asp-route-value="@item.Id" 
           class="list-group-item">@item.Name</a>
    }
</div>